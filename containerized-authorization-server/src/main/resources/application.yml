server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql8}:3306/${DB_NAME}
    username: ${DB_PASSWORD}
    password: ${DB_USERNAME}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
