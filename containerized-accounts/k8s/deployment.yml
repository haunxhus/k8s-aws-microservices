apiVersion: apps/v1
kind: Deployment
metadata:
  name: k8s-service-accounts
  namespace: k8s-containerized-services
  labels:
    app: k8s-service-accounts
spec:
  replicas: 3
  selector:
    matchLabels:
      app: k8s-service-accounts
  template:
    metadata:
      labels:
        app: k8s-service-accounts
    spec:
      #loadBalancerClass: nil 
      containers:
        - name: containerized-accounts
          image: linhpv5555/containerized-accounts:latest
          #imagePullPolicy: Never
          ports:
            - containerPort: 2222
            #name: service-accounts
#          livenessProbe:
#            httpGet:
#              path: /health
#              port: 8080
#              scheme: HTTP
#            initialDelaySeconds: 180
#          readinessProbe:
#            httpGet:
#              path: /health
#              port: 8080
#              scheme: HTTP
#            initialDelaySeconds: 10

          #args:
          #  - "-apptype=accounts"